# Migration `20200427032037-init`

This migration has been generated by rainy-me at 4/27/2020, 3:20:37 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."User" (
    "avatar" text  NOT NULL ,
    "createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "email" text  NOT NULL ,
    "id" integer  NOT NULL ,
    "name" text  NOT NULL ,
    "password" text  NOT NULL ,
    "updatedAt" timestamp(3)  NOT NULL ,
    "userId" integer   ,
    PRIMARY KEY ("id")
) 

CREATE TABLE "public"."Chat" (
    "id" SERIAL,
    "title" text   ,
    PRIMARY KEY ("id")
) 

CREATE TABLE "public"."Message" (
    "chatId" integer   ,
    "content" text  NOT NULL ,
    "createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "id" SERIAL,
    "sent" boolean  NOT NULL DEFAULT false,
    "updatedAt" timestamp(3)  NOT NULL ,
    "viewed" boolean  NOT NULL DEFAULT false,
    PRIMARY KEY ("id")
) 

CREATE TABLE "public"."Call" (
    "endAt" timestamp(3)  NOT NULL ,
    "id" SERIAL,
    "startAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY ("id")
) 

CREATE TABLE "public"."_UserChats" (
    "A" integer  NOT NULL ,
    "B" integer  NOT NULL 
) 

CREATE UNIQUE INDEX "User.email" ON "public"."User"("email")

CREATE UNIQUE INDEX "_UserChats_AB_unique" ON "public"."_UserChats"("A","B")

CREATE  INDEX "_UserChats_B_index" ON "public"."_UserChats"("B")

ALTER TABLE "public"."User" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."Message" ADD FOREIGN KEY ("chatId")REFERENCES "public"."Chat"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."_UserChats" ADD FOREIGN KEY ("A")REFERENCES "public"."Chat"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."_UserChats" ADD FOREIGN KEY ("B")REFERENCES "public"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200427032037-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,44 @@
+datasource db {
+  provider = "postgresql"
+  url      = env("DATABASE_URL")
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model User {
+  id        Int      @id
+  email     String   @unique
+  name      String
+  password  String
+  avatar    String
+  contacts  User[]   @relation("UserToUser")
+  chats     Chat[]   @relation("UserChats", references: [id])
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  userId    Int?
+}
+
+model Chat {
+  id       Int       @default(autoincrement()) @id
+  user     User[]    @relation("UserChats", references: [id])
+  title    String?
+  messages Message[]
+}
+
+model Message {
+  id        Int      @default(autoincrement()) @id
+  content   String
+  sent      Boolean  @default(false)
+  viewed    Boolean  @default(false)
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  chatId    Int?
+}
+
+model Call {
+  id      Int      @default(autoincrement()) @id
+  startAt DateTime @default(now())
+  endAt   DateTime
+}
```


